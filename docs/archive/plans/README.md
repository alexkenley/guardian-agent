# Archived Plans

This directory holds completed or superseded implementation plans whose live behavior is now documented in a current design, as-built spec, or newer active plan.

Use `docs/plans/` for active or intentionally pending implementation work. Use `docs/implemented/` for proposal-era documents whose core product or architecture has shipped. Use this directory for one-time execution plans that are complete enough to no longer belong in the active planning set.
